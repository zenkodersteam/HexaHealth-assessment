# Assessment Submission

Two independent projects for the frontend and backend assessments.

- [`frontend-landing/`](frontend-landing) — Responsive product landing page (Next.js + TypeScript).
- [`backend-notes-api/`](backend-notes-api) — Notes CRUD REST API (NestJS) with an in-memory store and a demo HTML frontend.

Each project has its own README with setup/run instructions.

## Quick start

```bash
# Frontend — http://localhost:3000
cd frontend-landing
npm install
npm run dev

# Backend — http://localhost:3001
cd backend-notes-api
npm install
npm run start:dev
```

## Submission checklist

- [ ] Push this folder to a GitHub repository and add the link here.
- [ ] Record a screen capture showing:
  - Landing page + responsive resize/device emulation (frontend).
  - Creating, retrieving, updating, and deleting a note (backend, via the demo page at `http://localhost:3001` or curl/Postman).
- [ ] Add the video link here.

**GitHub repo:** _add link_
**Demo video:** _add link_
